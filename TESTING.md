# FinderTerm 手動テストチェックリスト

前提: `./scripts/build-app.sh` でビルドし `build/FinderTerm.app` を起動。
アクセシビリティとオートメーション(Finder)の権限を許可済み。

## 基本(仕様: 成功基準)

- [ ] Finderウィンドウを開くと下半分にターミナルが出てプロンプトが表示される
- [ ] Finderウィンドウのドラッグ・リサイズにペインが遅延なく追従する
- [ ] Finderでフォルダ移動するとアイドルなシェルが自動cdされる
- [ ] `claude` (または `sleep 60`) 実行中にフォルダ移動してもcdされない
- [ ] claudeをCtrl+Cで終了→シェルはその場に留まり `claude --resume` でセッションが見つかる(v1.1: 自動再同期は廃止)
- [ ] ペイン内のclaudeで「Transcript saving is off」警告が**出ない**こと(v1.2: セッションマーカーの環境変数を無害化。自動テスト: testShellEnvironmentIsSanitizedFromClaudeSessionMarkers)
- [x] 30秒以上のコマンド終了後はアンカーされ、Finderでフォルダ移動しても自動cdされない(自動テスト済み: testAnchorAfterLongCommandAndReleaseByManualCd)
- [x] アンカー中に手動で `cd` すると追従が再開する(自動テスト済み)
- [ ] メニューバーのセッション一覧でアンカー中は⚓表示になり、サブメニューの「追従を再開」で解除→現在のFinderフォルダに追従する
- [ ] Finderウィンドウ2枚 → それぞれ独立したペイン・セッション
- [ ] ⌥⌘Tで**最前面のウィンドウ/タブのペインだけ**が切り替わる(他のウィンドウのペインは不変)
- [ ] メニューバーのセッション一覧に全ペイン(フォルダ+実行中コマンド)が並び、クリックでそのウィンドウ/タブが前面化する
- [ ] メニューバー「全ペインを表示」で全体のオン/オフ

## ライフサイクル

- [ ] アイドルなペインのFinderウィンドウを閉じる → ペインが黙って消える
- [ ] `sleep 60` 実行中のウィンドウを閉じる → 確認ダイアログ
- [ ] 「セッションを残す」→ タイトルバー付き独立ウィンドウになりプロセス継続
- [ ] 独立ウィンドウを閉じる(実行中)→ 再度確認ダイアログ
- [ ] 実行中セッションがある状態でアプリ終了(⌘Q)→ 件数付き一括確認
- [ ] `killall Finder` → 実行中セッションは確認ダイアログで「セッションを残す」を選ぶと独立ウィンドウ化されて生き残り、
      Finder復帰後の新ウィンドウには新しいペインが付く

## タブ

- [x] ⌘Tで新しいタブ → 1秒以内に新タブのフォルダでペインが出る(自動検証済み 2026-07-17)
- [x] 元のタブに戻る → 元のペイン(セッション維持)が再表示され、裏のタブのペインは隠れる(自動検証済み)
- [x] アプリ起動前から存在した(一度も表になっていない)タブへ切替 → 新規登録されてペインが付く(自動検証済み)
- [ ] タブごとに独立したセッションであること(タブAでclaude実行中にタブBへ切替→戻ってもセッション無傷)

## 入力・z順序

- [ ] ペインで⌘V(ペースト)・⌘C(コピー)・⌘A(全選択)が効く
- [x] ペインをクリック→Finderのファイル一覧をクリック(ウィンドウ前面化)→ペインが隠れても0.5秒以内に最前面へ復帰(自動検証済み 2026-07-17)
- [x] サイドバーのFavoritesクリックでフォルダ移動→アイドルなら自動cd(自動検証済み: shellのcwd変化を確認)

## 追従の網羅

- [ ] 最小化 → ペインが消える。復元 → 戻る
- [ ] 別Spaceへ移動(Mission Control)→ 元のSpaceのペインが他Spaceに出ない
- [ ] Finderをフルスクリーンにするとペインが隠れる
- [ ] ペイン境界のドラッグで高さが変わり、全ウィンドウ・再起動後も維持される
- [ ] 「最近の項目」へ移動 → cdされず、最後の有効フォルダのまま
- [ ] 情報ウィンドウ(⌘I)にはペインが付かない
- [ ] スリープ復帰・外部ディスプレイ抜き差し後も位置がずれない

## エラー系

- [ ] アクセシビリティ権限を剥奪 → 30秒以内にペインが隠れ、メニューバーが警告表示
- [ ] 権限を再許可 → 30秒以内に復帰
- [ ] オートメーション(Finder制御)を拒否 → 位置追従は継続するが自動cdだけ止まる(最後の有効フォルダを保持)
- [ ] ペインで `exit` → ペインが消える(ウィンドウは残る)
- [ ] `claude` をペインで起動して普通に対話できる(矢印キー・色・リサイズ)
